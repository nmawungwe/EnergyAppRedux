
export const initialState = {
    stats: [],
    loading: false,
    hasErrors: false,
}

export default function statsReducer(state=initialState, action){
    switch(action.type){
        default:
            return state
    }
}
